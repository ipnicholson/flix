<h1><%= @movies.size %> Movies Found:</h1>

<ul>
<% @movies.each do |movie| %>
  <li>
    <strong><%= movie.name %></strong> (<%= movie.rating %>): <%= number_to_currency(movie.total_gross) %>
  </li>
<% end %>
</ul>